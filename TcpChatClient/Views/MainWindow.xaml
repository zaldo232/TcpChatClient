<Window x:Class="TcpChatClient.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TcpChatClient.ViewModels"
        Title="TCP 채팅 클라이언트" Height="450" Width="400">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <DockPanel Margin="10">
        <ListBox ItemsSource="{Binding Messages}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Display}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="0,10,0,0">
            <TextBox Width="200" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="보내기" Width="60" Command="{Binding SendCommand}" Margin="5,0,0,0"/>
            <Button Content="파일" Width="60" Command="{Binding SendFileCommand}" Margin="5,0,0,0"/>
        </StackPanel>
    </DockPanel>
</Window>
